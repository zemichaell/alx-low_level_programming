#include "main.h"
/**
 * add_two_numbers - add two integer number
 * @x: any enteger number
 * @y: any enteger numbre
 * @add: opretor which add two numbers
 * Return: Always 0.
 */
int add(int x, int y)
{
return (x + y);
}
