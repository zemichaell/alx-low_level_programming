#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success/correct)
 */

int main(void)
{
char reverse

for (reverse = 122; reverse >= 97; reverse--)
{
putchar (reverse);

}
putchar ('\n');

return (0);
}
