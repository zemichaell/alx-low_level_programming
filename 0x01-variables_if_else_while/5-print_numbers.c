#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */

int main(void)

{
for (int digit = 0; digit <= 9; digit++)
{
printf("%ddigit", digit);
}
printf('\n');

return (0);

}
