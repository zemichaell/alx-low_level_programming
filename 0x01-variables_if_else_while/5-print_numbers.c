#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */

int main(void)
int digit;
{
for (digit = 0; digit <= 9; digit++)
{
printf("%ddigit", digit);
}
printf('\n');

return (0);

}
